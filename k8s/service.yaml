apiVersion: v1
kind: Service
metadata:
  name: hotel-restart-service
  labels:
    app: hotel-restart
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hotel-restart
